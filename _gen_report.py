# -*- coding: utf-8 -*-
"""生成《多视觉融合的三维重建平台搭建 需求分析报告》Word 文档"""
from docx import Document
from docx.shared import Pt, RGBColor, Cm
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.enum.table import WD_TABLE_ALIGNMENT
from docx.oxml.ns import qn

SONG = u'宋体'
BLACK = RGBColor(0, 0, 0)

doc = Document()

# ---------- 全局样式 ----------
normal = doc.styles['Normal']
normal.font.name = 'Times New Roman'
normal.font.size = Pt(10.5)          # 五号
normal.font.color.rgb = BLACK
normal._element.rPr.rFonts.set(qn('w:eastAsia'), SONG)

# 标题样式统一为宋体黑字（颜色、字体；字号在各标题 add 时单独设）
for level, size in [(1, 14), (2, 12), (3, 10.5)]:   # 四号 / 小四 / 五号
    st = doc.styles[f'Heading {level}']
    st.font.name = SONG
    st.font.color.rgb = BLACK
    st._element.rPr.rFonts.set(qn('w:eastAsia'), SONG)
    st.font.size = Pt(size)
    st.font.bold = True


def _set(run, size=10.5, bold=False, name=SONG):
    run.font.name = name
    run.font.size = Pt(size)
    run.font.bold = bold
    run.font.color.rgb = BLACK
    run._element.rPr.rFonts.set(qn('w:eastAsia'), name)


def para(text='', size=10.5, bold=False, align=None, indent=True):
    p = doc.add_paragraph()
    r = p.add_run(text)
    _set(r, size=size, bold=bold)
    if align is not None:
        p.alignment = align
    if indent and text:
        p.paragraph_format.first_line_indent = Pt(21)  # 首行缩进 2 字符
    return p


_BULLET_N = 0


def bullet(text, size=10.5):
    global _BULLET_N
    _BULLET_N += 1
    p = doc.add_paragraph()
    r = p.add_run(f'（{_BULLET_N}）{text}')
    _set(r, size=size)
    p.paragraph_format.first_line_indent = Pt(21)
    return p


def h1(text):
    global _BULLET_N
    _BULLET_N = 0
    p = doc.add_heading('', level=1)
    r = p.add_run(text)
    _set(r, size=14, bold=True)
    return p


def h2(text):
    global _BULLET_N
    _BULLET_N = 0
    p = doc.add_heading('', level=2)
    r = p.add_run(text)
    _set(r, size=12, bold=True)
    return p


def h3(text):
    global _BULLET_N
    _BULLET_N = 0
    p = doc.add_heading('', level=3)
    r = p.add_run(text)
    _set(r, size=10.5, bold=True)
    return p


def caption(text):
    p = doc.add_paragraph()
    r = p.add_run(text)
    _set(r, size=10.5, bold=True)
    p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    return p


def table(rows, header=True, widths=None):
    """rows: list of list of str"""
    t = doc.add_table(rows=len(rows), cols=len(rows[0]))
    t.style = 'Table Grid'
    t.alignment = WD_TABLE_ALIGNMENT.CENTER
    for i, row in enumerate(rows):
        for j, cell in enumerate(row):
            c = t.cell(i, j)
            c.text = ''
            p = c.paragraphs[0]
            r = p.add_run(str(cell))
            _set(r, size=9, bold=(header and i == 0))
            if header and i == 0:
                p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    if widths:
        for j, w in enumerate(widths):
            for i in range(len(rows)):
                t.cell(i, j).width = Cm(w)
    return t


def usecase_table(fields):
    """用例描述表：两列（字段名 | 描述）"""
    rows = [['字段', '内容']]
    for k, v in fields:
        rows.append([k, v])
    t = table(rows, header=True, widths=[3.2, 12.8])
    return t


# ============================================================
# 封面标题
# ============================================================
p = doc.add_paragraph(); p.alignment = WD_ALIGN_PARAGRAPH.CENTER
r = p.add_run('多视觉融合的三维重建平台搭建')
_set(r, size=22, bold=True)
p = doc.add_paragraph(); p.alignment = WD_ALIGN_PARAGRAPH.CENTER
r = p.add_run('需 求 分 析 报 告')
_set(r, size=22, bold=True)

p = doc.add_paragraph(); p.alignment = WD_ALIGN_PARAGRAPH.CENTER
r = p.add_run('电子科技大学 信息与软件工程学院')
_set(r, size=12)
p = doc.add_paragraph(); p.alignment = WD_ALIGN_PARAGRAPH.CENTER
r = p.add_run('课程：数字动漫综合训练III    指导教师：王庆先')
_set(r, size=10.5)
doc.add_paragraph()

# ============================================================
# 1. 引言
# ============================================================
h1('1. 引言')
para('本报告对“多视觉融合的三维重建平台搭建”课题进行需求分析与可行性研究。该课题要求基于香橙派与多摄像头搭建多视觉图像采集端，通过 TCP 网络通信与上位机交互，在上位机完成从图片管理、图像处理、三维重建计算、模型管理到展示交互的完整三维扫描重建流程，最终交付一套可运行、可演示、可复现的多视觉三维重建软硬件平台。')

h2('1.1 编写目的')
para('本报告旨在完成课题任务书中第一项任务“对多视觉三维重建原型需求分析与可行性研究”。具体目标如下：')
bullet('调研多视图三维重建、嵌入式图像采集、点云建模、软件模块化开发四类关键技术，梳理各自的主流技术方案；')
bullet('分析上述技术在本课题工程化落地过程中的现实痛点与约束；')
bullet('在调研基础上明确系统的功能需求与非功能需求；')
bullet('为后续总体设计、详细设计、三维扫描系统实现以及 TDD 测试驱动开发提供需求依据与验收基准。')

h2('1.2 项目风险')
para('本项目相关风险承担者及其职责如下：')
bullet('任务提出者（指导教师）：需在需求分析阶段明确课题范围、验收标准与评价等级（优/良/中/差），确保需求准确、可行、完整。')
bullet('开发团队（四人小组）：需理解需求并评估技术可行性，重点把控硬件采购调试、网络通信、密集重建算法三大风险，按计划推进。')
bullet('产品使用者（演示/评审人员）：需提出合理的使用场景与操作预期，用于校验系统是否满足目标。')
para('本项目主要风险点包括：香橙派与多摄像头的硬件选型与驱动兼容风险、多路视频同步采集与 TCP 上下位机通信的可靠性风险、从稀疏到密集重建的算法复杂度与精度风险，以及本机开发环境（Python 3.14 无 open3d 可用 wheel）带来的第三方库兼容风险。上述风险将在 2.5 节与 2.6 节进一步量化。')

h2('1.3 文档约定')
bullet('正文风格：宋体，五号（10.5pt）。')
bullet('一级标题：宋体，四号（14pt），加粗。')
bullet('二级标题：宋体，小四（12pt），加粗。')
bullet('三级标题：宋体，五号（10.5pt），加粗。')
para('本文档涉及的常用术语与缩写约定如下表所示。')
table([
    ['术语/缩写', '含义'],
    ['SfM', 'Structure from Motion，运动恢复结构，由多张二维图像恢复相机位姿与三维稀疏点云'],
    ['MVS', 'Multi-View Stereo，多视图立体匹配，用于由稀疏点云生成密集点云'],
    ['PnP', 'Perspective-n-Point，由若干 2D-3D 对应点求解相机位姿'],
    ['BA', 'Bundle Adjustment，光束法平差，联合优化三维点与相机参数以降低重投影误差'],
    ['TCP', 'Transmission Control Protocol，传输控制协议，本课题上下位机通信方式'],
    ['SIFT', 'Scale-Invariant Feature Transform，尺度不变特征变换'],
    ['PLY/OBJ/glTF', '点云与三维模型常见存储/交换格式'],
    ['TDD', 'Test-Driven Development，测试驱动开发'],
    ['上位机/下位机', '上位机指承担计算与展示的 PC；下位机指香橙派采集端'],
], header=True)

h2('1.4 预期读者和阅读建议')
bullet('指导教师：重点阅读第 2 章技术调研与第 5、6 章需求，评估方案设计是否完整、可行性论证是否充分。')
bullet('开发团队（计算/后端/前端/测试四人）：重点阅读第 2 章技术选型与第 5 章用例，据此进行模块划分与编码。')
bullet('测试人员：重点阅读第 5 章功能需求与第 6 章非功能需求，据此设计单元/集成/系统测试用例。')
bullet('文档编写人员：遵循 1.3 节文档约定，并对本报告持续补充完善。')

h2('1.5 产品范围')
para('本产品的范围是一套多视觉融合的三维扫描重建软硬件平台，其核心业务流程为：由部署于香橙派的多摄像头采集目标物体的多视角图像序列，经 TCP 网络传输至上位机，上位机依次完成图片管理、图像处理（特征提取与匹配）、三维重建计算（相机参数估计、稀疏点云、密集重建、模型优化）、模型管理（点云/网格的保存与导出），最终在 Web 前端进行三维模型展示与交互。')
para('产品“做什么”：多视角图像采集与传输、图像管理与处理、稀疏重建、密集重建、模型优化、模型管理与三维展示。')
para('产品“不做什么”（超出本期范围或仅作可选进阶）：NeRF/3D Gaussian Splatting 等新视角合成类神经渲染方法、云端分布式重建、高精度工业级量测标定。上述内容在 6.5 节作为可拓展功能单列。')

h2('1.6 参考文献')
bullet('《Software Engineering：A Practitioner’s Approach》，Roger S. Pressman，机械工业出版社')
bullet('《Multiple View Geometry in Computer Vision》，R. Hartley, A. Zisserman，Cambridge University Press')
bullet('《Structure-from-Motion Revisited》，J. L. Schönberger, J.-M. Frahm，CVPR 2016（COLMAP）')
bullet('《OpenMVG：An Open Multiple View Geometry Library》，P. Moulon et al.')
bullet('《A Comparison and Evaluation of Multi-View Stereo Reconstruction Algorithms》，S. M. Seitz et al.，CVPR 2006')
bullet('《软件项目报告模板》，邱元杰，电子科技大学')
bullet('《软件工程：实践者的研究方法》，[美] 普雷斯曼，机械工业出版社')
bullet('OpenCV、COLMAP、OpenMVS、Open3D、Three.js 官方文档与开源代码')

# ============================================================
# 2. 综合描述
# ============================================================
h1('2. 综合描述')
para('本章对多视觉融合的三维重建平台的作用范围、技术现状与选型、功能组成、用户、运行环境、限制与约束进行概述，其中 2.1 节集中完成课题要求的技术调研与痛点梳理。')

h2('2.1 产品的状况（技术调研与选型）')
para('上学期已实现一套经典的稀疏 SfM 原型（OpenCV），流水线为 SIFT 特征提取 → 顺序特征匹配 → 本质矩阵恢复位姿 → PnP 增量重建 → 三角化 → 光束法平差 → 稀疏点云输出。该原型在本学期将作为三维重建计算模块的算法基础，并需在其上补齐密集重建、模型优化与完整工程化封装。以下按课题要求，分别调研四类关键技术。')

h3('2.1.1 多视图三维重建技术调研')
para('多视图三维重建的基本原理是基于多视图几何：利用不同视角图像间的特征对应关系，通过极线约束估计相机相对位姿，再通过三角化恢复三维点坐标，最终经光束法平差联合优化得到稀疏点云与相机位姿。其经典技术流程为“特征提取 → 特征匹配 → 相机参数估计（运动恢复结构）→ 稀疏点云 → 密集重建 → 模型优化”。')
para('按运动恢复结构（SfM）的实现策略，主流方案可分为增量式、全局式与混合式三类；按特征描述方式，可分为传统手工特征与基于深度学习的特征匹配；密集重建则主要依赖多视图立体匹配（MVS）。主流技术方案对比如下表所示。')
table([
    ['方案/工具', '类型', '特点', '适用性与工程成本'],
    ['COLMAP', '增量式 SfM + MVS', '行业标杆，鲁棒性强、精度高，支持图形界面与命令行，有 Python 绑定', '精度高但增量式 BA 计算量随图像数增长，小数据集运行良好'],
    ['OpenMVG', '全局式 SfM', '旋转/平移全局求解，速度快，模块化接口清晰', '对误匹配敏感，鲁棒性略弱于增量式，适合快速原型'],
    ['OpenMVS', '密集 MVS', '补全稠密点云、网格重建与纹理映射', '需先有稀疏点云与位姿，与 COLMAP 配套使用'],
    ['Open3D/PCL', '点云处理库', '点云配准、降采样、法向估计、泊松/alpha shape 表面重建', 'Open3D 在 py3.14 无可用 wheel，需降级环境或自实现'],
    ['SuperPoint+SuperGlue', '深度特征+匹配', '学习型特征，弱纹理/光照变化下匹配更鲁棒', '需 GPU 推理，8GB 显存可运行，工程集成成本较高'],
    ['神经渲染(NeRF/3DGS)', '新视角合成', '可生成高质量新视角图像与几何', '训练耗时、需 GPU、非本课题主线，列为可选进阶'],
], header=True, widths=[3.6, 2.4, 5.0, 5.0])
para('工程落地痛点如下：其一，增量式 SfM 存在误差累积与漂移，环形小基线数据集（如本课题 Temple Ring 类数据）存在深度歧义，易导致点云在深度方向拉长；其二，传统特征在弱纹理、重复纹理、反光表面下匹配失效；其三，相机内参若不准确（未标定或尺寸缩放错误）将导致整体几何失真，虽 SfM 内部自洽但对外部量测消费不准确；其四，全局 BA 计算量大，大规模场景内存与耗时陡增。')

h3('2.1.2 嵌入式图像采集技术调研')
para('嵌入式图像采集端由香橙派（ARM 架构单板计算机，类似树莓派）加多路摄像头构成。香橙派运行 Linux 系统，通过 CSI/MIPI 或 USB 接口接入摄像头。图像采集的底层方案主要有 V4L2（Video for Linux 2）驱动接口、GStreamer 多媒体框架以及 OpenCV 的 VideoCapture 封装。主流方案对比如下表。')
table([
    ['方案', '方式', '特点', '工程成本'],
    ['V4L2', '系统级驱动接口', '底层、灵活、延迟低，可精确控制帧率/格式/分辨率', '接口较底层，需自行封装，调试成本高'],
    ['GStreamer', '多媒体流水线框架', '插件化、支持硬件编解码，适合多路视频流', '学习曲线较陡，依赖较多'],
    ['OpenCV VideoCapture', '高层封装', '接口简单、开发快，与重建算法同技术栈', '多路并发与低延迟控制能力弱于 V4L2'],
    ['TCP + 自定义协议', '网络传输', '上下位机解耦，支持多设备数据汇总', '需自定义协议与断线重连、粘包处理'],
], header=True, widths=[3.2, 3.0, 6.0, 4.0])
para('多摄像头同步采集是本课题的关键难点：多路摄像头需保证拍摄同一物体时视角的时序一致性。同步方式包括硬件外触发（外同步信号）与软件时间戳同步（软同步）两类。工程落地痛点包括：多路视频的 USB 带宽争用与帧率下降、软同步精度不足导致不同视角图像错位、摄像头内参/外参需预先标定、香橙派 ARM 算力有限无法承担重建计算（故重计算必须置于上位机）、驱动兼容性与供电散热问题。')

h3('2.1.3 点云建模技术调研')
para('点云建模涵盖从稀疏点云到密集点云、再到带纹理网格模型的完整链条。核心步骤为：MVS 密集重建 → 点云去噪/降采样 → 法向估计 → 表面重建 → 网格简化/补洞 → 纹理映射。表面重建的常用算法包括泊松表面重建（Poisson）、Delaunay 三角化、alpha shape、Marching Cubes（体素/TSDF 融合）与 Ball Pivoting 等。主流方案对比如下表。')
table([
    ['方案', '方法', '特点', '工程成本'],
    ['Open3D', '点云处理 + 泊松重建', '接口丰富，一站式完成法向/重建/简化', 'py3.14 无 wheel，需 py3.10~3.12 环境'],
    ['PCL', 'C++ 点云库', '功能全面、工业级', 'C++ 依赖重，与 Python 流水线集成成本高'],
    ['numpy+scipy 自实现', 'Delaunay/alpha shape/体素', '零第三方依赖，契合本机 py3.14 环境', '需自行实现与调参，大规模点云需分块处理'],
    ['OpenMVS', 'MVS + 网格 + 纹理', '稠密重建与纹理映射一体化', '独立 C++ 程序，需以命令行/子进程集成'],
], header=True, widths=[3.2, 3.2, 6.0, 4.2])
para('工程落地痛点包括：稀疏点云存在离群点与噪声，直接表面重建会产生伪面；表面重建依赖点云法向量，法向估计质量直接影响网格质量；Delaunay 三角化在数万点以上会出现内存爆炸，需分块或先降采样；纹理映射需要稳定 UV 展开；Web 展示需将点云/网格转换为 glTF/GLB 格式。')

h3('2.1.4 软件模块化开发技术调研')
para('本课题在软件工程层面需将重建算法封装为可维护、可测试、可扩展的系统。主流工程化思路为分层架构 + 前后端分离 + 异步任务调度 + TDD。典型分层为表现层（Web 前端）、业务层（后端服务）、数据层（存储）与计算层（重建算法）四层。主流方案对比如下表。')
table([
    ['方面', '方案', '特点', '本课题选择'],
    ['架构', '分层/模块化', '高内聚低耦合，接口清晰', '按五大模块划分'],
    ['前后端', 'REST + WebSocket', '前后端解耦，状态推送友好', 'REST API + 任务状态轮询/推送'],
    ['任务调度', 'Pipeline/消息队列', '长耗时重建任务异步化', '任务队列（Python 队列/Celery 轻量替代）'],
    ['测试', 'TDD', '先写用例后写实现', 'pytest 单元/集成/系统测试'],
    ['通信', 'TCP 自定义协议', '上下位机可靠传输', 'TCP + 定长/JSON 消息'],
], header=True, widths=[2.4, 3.6, 5.0, 4.6])
para('工程落地痛点包括：重建算法属于计算密集型长任务，需异步执行并向前端反馈进度；算法模块（Python）与采集模块（可能涉及 C++）跨语言集成需统一接口；模块间数据契约（图像、点云、模型格式）需严格定义；异常处理、日志与可观测性在长链路（采集→传输→计算→展示）中尤为关键。')

h3('2.1.5 技术选型结论')
para('综合上述调研与本机环境约束（Python 3.14、RTX 5060 8GB、无 conda、open3d 无 wheel），本课题的技术选型结论如下表所示。')
table([
    ['技术域', '选型结论', '理由'],
    ['特征提取/匹配', 'SIFT + BFMatcher（沿用上学期）', '已实现且稳定，依赖齐备'],
    ['相机参数估计', '本质矩阵 + PnP 增量式（沿用）', '已实现，补齐内参自标定后更稳'],
    ['稀疏点云', '三角化 + BA（scipy least_squares）', '已实现，重投影误差约 0.22px'],
    ['密集重建/点云建模', 'numpy+scipy 自实现（Delaunay/alpha shape/体素）', '规避 py3.14 无 open3d wheel 的限制'],
    ['嵌入式采集', 'OpenCV 采集 + TCP 自定义协议', '开发快、与算法同栈、可靠传输'],
    ['软件工程', '分层架构 + 前后端分离 + TDD', '满足课程软件工程与测试要求'],
], header=True, widths=[3.4, 6.0, 6.2])

h2('2.2 产品的功能')
para('依据课题任务书，系统划分为图片管理、图像处理、三维重建计算、模型管理、展示交互五大功能模块，并辅以用户管理与上下位机通信作为支撑。各模块功能概述如下：')
bullet('图片管理模块：多视角图像的采集控制、上传、存储、浏览与删除，以及采集任务的发起与状态管理。')
bullet('图像处理模块：图像预处理（灰度化、去畸变等）、特征提取（SIFT）、特征匹配（相邻帧匹配）。')
bullet('三维重建计算模块：相机参数估计（本质矩阵/PnP）、稀疏点云生成、密集重建、模型优化（BA/点云过滤），并以异步任务形式执行。')
bullet('模型管理模块：点云/网格的保存、导出（PLY/OBJ/glTF）、历史模型的管理与加载。')
bullet('展示交互模块：Web 前端三维模型展示（Three.js），支持旋转、缩放、平移等交互。')
para('各模块的功能与用户角色的对应关系见 2.3 节角色-功能矩阵。')

h2('2.3 用户类和特性')
para('本平台的主要用户分为三类：采集操作员、管理员、普通浏览者。各类用户的特性如下表所示。')
table([
    ['用户类', '主要操作', '特性'],
    ['采集操作员', '发起采集、上传图像、启动重建、查看重建进度', '需了解采集流程，可执行重建任务'],
    ['管理员', '用户管理、项目/模型管理、系统配置与硬件管理', '拥有全部功能权限，可增删用户与模型'],
    ['浏览者', '查看已完成的三维模型并交互', '只读权限，可旋转缩放查看模型'],
], header=True, widths=[3.0, 7.0, 5.6])
para('各角色可使用的功能以角色-功能矩阵表示如下（√ 表示拥有该功能）。')
table([
    ['功能', '采集操作员', '管理员', '浏览者'],
    ['用户登录/鉴权', '√', '√', '√'],
    ['图像采集控制', '√', '√', ''],
    ['图像上传/管理', '√', '√', ''],
    ['图像处理', '√', '√', ''],
    ['三维重建计算', '√', '√', ''],
    ['模型管理/导出', '√', '√', ''],
    ['模型三维展示', '√', '√', '√'],
    ['用户与系统管理', '', '√', ''],
], header=True, widths=[5.0, 3.6, 3.0, 3.0])

h2('2.4 运行环境')
bullet('下位机（采集端）：香橙派开发板（ARM 架构，运行 Linux 系统），外接多路摄像头，通过 TCP 与上位机通信。')
bullet('上位机（计算与展示端）：64 位 Windows 10 及以上系统，CPU 主频 2.0GHz 及以上、内存 8GB 及以上（推荐 16GB），具备 NVIDIA GPU（本机为 RTX 5060 8GB）以加速特征与 BA 计算。')
bullet('开发环境：PyCharm（IDE），Python 3.14，主要依赖 OpenCV 4.13、NumPy、SciPy、Numba 等。')
bullet('软件环境：后端服务框架（如 Flask/FastAPI）、前端 Three.js、数据库（如 SQLite）用于元数据存储。')
bullet('网络环境：采集端与上位机处于同一局域网，TCP 连接稳定、延迟低，带宽满足多路图像传输需求。')

h2('2.5 设计和实现上的限制')
bullet('开发语言：以 Python 为主，采集端驱动如需要可辅以 C/C++；算法层复用上学期 Python + OpenCV 实现。')
bullet('环境限制：本机 Python 3.14 无法安装 open3d（无可用 wheel），密集重建与点云建模须采用 numpy+scipy 自实现或另建 py3.10~3.12 环境，详见 2.1 节选型。')
bullet('相机内参：上学期原型存在内参缩放问题（官方 3072×2048 内参用于 640×480 图像），本学期需补齐自标定/EXIF 内参读取或正确缩放，详见代码 TODO 与 6.1 节。')
bullet('数据规模限制：单次重建图像数量与点云规模受内存约束，密集重建需引入降采样与分块策略。')
bullet('平台限制：采集端香橙派算力有限，三维重建计算全部在上位机完成；下位机仅负责采集与传输。')

h2('2.6 假设和约束（依赖）')
bullet('假设香橙派与多摄像头硬件按计划到位且驱动可用，摄像头可完成基本标定。')
bullet('假设采集端与上位机处于同一局域网，网络连接稳定，支持 TCP 长连接。')
bullet('假设用户具备基本的计算机操作水平，能按说明使用系统。')
bullet('约束：项目在课程规定周期内完成，遵循 TDD 开发流程，交付系统、代码、文档与答辩 PPT。')
bullet('约束：本机开发环境以 Python 3.14 为基准，尽量避免引入需要 conda/WSL2 的重依赖。')

# ============================================================
# 3. 外部接口需求
# ============================================================
h1('3. 外部接口需求')

h2('3.1 用户界面')
para('系统采用 Web 浏览器作为统一用户界面，设计目标为简单、清晰、易用。界面布局突出功能重点，错误信息以弹窗提示。主要界面原型如下（与实际开发可能有细微差异）：')
h3('3.1.1 登录界面')
bullet('账号输入框：输入用户名。')
bullet('密码输入框：输入密码。')
bullet('登录按钮：点击后校验身份，跳转至对应主界面。')
h3('3.1.2 图片管理界面')
bullet('采集控制区：发起/停止多摄像头采集任务，显示采集状态与进度。')
bullet('图片列表区：以缩略图列表展示已采集/上传的图像，支持批量选择。')
bullet('上传按钮：从本地选择图像上传。')
bullet('删除按钮：删除选中图像。')
h3('3.1.3 重建任务界面')
bullet('任务发起：选择图像集、设置重建参数（是否密集重建、降采样比例等），提交重建任务。')
bullet('进度展示：以进度条展示特征提取、匹配、重建、优化各阶段进度。')
h3('3.1.4 模型展示界面')
bullet('三维视图区：基于 Three.js 加载并展示重建模型，支持鼠标拖拽旋转、滚轮缩放、右键平移。')
bullet('模型信息区：显示点数/面数、重投影误差、生成时间等元数据。')
bullet('导出区：导出 PLY/OBJ/glTF 格式模型。')

h2('3.2 软件接口')
para('系统的软件接口主要包括上下位机通信接口、后端服务接口与第三方库接口，描述如下。')
h3('3.2.1 上下位机 TCP 通信接口')
para('采集端（下位机）与上位机之间采用 TCP 长连接通信，消息采用 JSON 文本承载，帧边界以长度前缀或换行分隔，需处理粘包/半包与断线重连。主要消息类型如下表所示。')
table([
    ['消息类型', '方向', '说明'],
    ['采集指令', '上位机→下位机', '下发采集参数（分辨率、帧数、摄像头编号等），启动/停止采集'],
    ['图像数据', '下位机→上位机', '上传单帧或多帧图像数据（可压缩）'],
    ['状态/心跳', '双向', '上报采集状态、设备在线状态、异常信息'],
    ['采集完成', '下位机→上位机', '通知采集任务完成，触发上位机进入重建流程'],
], header=True, widths=[3.4, 3.4, 8.8])
h3('3.2.2 后端服务接口')
para('上位机后端以 REST API 形式向前端提供能力，主要接口包括：用户登录/鉴权接口、图像上传与列表接口、重建任务创建/查询/进度接口、模型列表/下载接口等。数据以 JSON 交换，图像与模型以二进制文件方式传输。')
h3('3.2.3 第三方库接口')
bullet('OpenCV：提供特征提取、匹配、位姿求解、图像读写等能力。')
bullet('NumPy/SciPy：提供数值计算、优化（least_squares）与几何处理能力。')
bullet('Numba CUDA：提供重投影误差并行计算的 GPU 加速能力。')
bullet('Three.js：提供前端 glTF/GLB 模型的加载与渲染能力。')

# ============================================================
# 4. 系统用例设计
# ============================================================
h1('4. 系统用例设计')
para('系统顶层用例涵盖采集、管理、处理、重建、展示与用户管理六大类，按五大功能模块与支撑模块组织。系统用例清单如下表所示，详细描述见 5.1 节。')
table([
    ['用例编号', '用例名', '所属模块', '主要参与者'],
    ['UC-01', '用户登录', '用户管理', '采集操作员/管理员/浏览者'],
    ['UC-02', '图像采集', '图片管理', '采集操作员/管理员'],
    ['UC-03', '图像上传', '图片管理', '采集操作员/管理员'],
    ['UC-04', '图像浏览与删除', '图片管理', '采集操作员/管理员'],
    ['UC-05', '图像处理（特征提取与匹配）', '图像处理', '采集操作员/管理员'],
    ['UC-06', '三维重建（稀疏→密集→优化）', '三维重建计算', '采集操作员/管理员'],
    ['UC-07', '模型保存与导出', '模型管理', '采集操作员/管理员'],
    ['UC-08', '模型三维展示与交互', '展示交互', '全部用户'],
    ['UC-09', '用户与系统管理', '用户管理', '管理员'],
], header=True, widths=[2.2, 6.0, 3.6, 3.8])
para('图 4-1 为系统总体用例图（由上述用例清单与参与者关系构成，详见 5.1 节各用例描述）。')

# ============================================================
# 5. 系统功能需求
# ============================================================
h1('5. 系统功能需求')

h2('5.1 用例描述')
para('本节对系统的核心用例进行具体描述。')

caption('表 5-1 UC-01 用户登录用例描述表')
usecase_table([
    ('用例名', '用户登录'),
    ('简述', '用户通过账号密码登录系统，进入对应角色的功能界面。'),
    ('主要参与者', '采集操作员、管理员、浏览者。'),
    ('情景目标', '实现身份鉴权，使不同角色可使用其权限对应的功能。'),
    ('前提条件', '系统已部署运行，用户账号已存在。'),
    ('触发事件', '用户打开系统登录页并提交登录。'),
    ('场景描述', '输入账号 → 输入密码 → 选择/系统识别身份 → 点击登录 → 校验通过后进入主界面。'),
    ('异常处理', '账号或密码错误时弹出提示；连续多次失败可锁定或限流。'),
    ('后置条件', '登录成功后显示对应角色的主界面。'),
])

caption('表 5-2 UC-02 图像采集用例描述表')
usecase_table([
    ('用例名', '图像采集'),
    ('简述', '控制香橙派多摄像头采集目标物体的多视角图像序列，并经 TCP 传输至上位机。'),
    ('主要参与者', '采集操作员、管理员。'),
    ('情景目标', '获取可用于三维重建的多视角图像序列。'),
    ('前提条件', '下位机在线、摄像头可用、TCP 连接建立。'),
    ('触发事件', '操作员点击“开始采集”。'),
    ('场景描述', '下发采集参数 → 下位机多路采集 → 图像经 TCP 上传 → 上报进度与完成状态 → 图片入库。'),
    ('异常处理', '连接中断时提示并支持重连续传；采集失败时上报错误并允许重试。'),
    ('后置条件', '采集图像保存至图片管理模块，可供后续处理与重建。'),
])

caption('表 5-3 UC-03 图像上传用例描述表')
usecase_table([
    ('用例名', '图像上传'),
    ('简述', '用户从本地选择图像上传至系统，作为重建输入。'),
    ('主要参与者', '采集操作员、管理员。'),
    ('情景目标', '扩充可用于重建的图像数据源。'),
    ('前提条件', '用户已登录。'),
    ('触发事件', '用户点击“上传图像”。'),
    ('场景描述', '选择本地图像 → 上传 → 校验格式与分辨率 → 入库并显示于图片列表。'),
    ('异常处理', '格式不支持或上传失败时提示，不写入损坏数据。'),
    ('后置条件', '上传图像出现在图片列表，可被选择参与重建。'),
])

caption('表 5-4 UC-04 图像浏览与删除用例描述表')
usecase_table([
    ('用例名', '图像浏览与删除'),
    ('简述', '用户浏览已采集/上传的图像，并可删除不再需要的图像。'),
    ('主要参与者', '采集操作员、管理员。'),
    ('情景目标', '管理图像数据，保持数据整洁。'),
    ('前提条件', '用户已登录，存在图像数据。'),
    ('触发事件', '用户进入图片管理界面。'),
    ('场景描述', '浏览缩略图列表 → 选中图像 → 点击删除 → 确认后删除。'),
    ('异常处理', '删除前二次确认；删除失败时提示并保持原数据。'),
    ('后置条件', '被删除图像从列表与存储中移除。'),
])

caption('表 5-5 UC-05 图像处理用例描述表')
usecase_table([
    ('用例名', '图像处理（特征提取与匹配）'),
    ('简述', '对选定图像集执行预处理、SIFT 特征提取与相邻帧特征匹配。'),
    ('主要参与者', '采集操作员、管理员。'),
    ('情景目标', '为三维重建提供特征点、描述子与匹配关系。'),
    ('前提条件', '存在有效图像集。'),
    ('触发事件', '发起重建任务，进入图像处理阶段。'),
    ('场景描述', '图像灰度化 → 特征提取 → 特征匹配 → 输出特征与匹配结果。'),
    ('异常处理', '特征点不足的图像剔除并告警；无有效匹配时终止并提示。'),
    ('后置条件', '生成特征点、描述子与匹配关系，供重建模块使用。'),
])

caption('表 5-6 UC-06 三维重建用例描述表')
usecase_table([
    ('用例名', '三维重建（稀疏→密集→优化）'),
    ('简述', '由匹配关系估计相机参数，生成稀疏点云，经密集重建与模型优化得到最终模型。'),
    ('主要参与者', '采集操作员、管理员。'),
    ('情景目标', '生成可展示的三维重建模型。'),
    ('前提条件', '图像处理阶段已产出特征与匹配结果。'),
    ('触发事件', '重建任务进入三维重建计算阶段。'),
    ('场景描述', '相机参数估计（本质矩阵/PnP）→ 稀疏点云三角化 → BA 优化 → 密集重建 → 模型优化。'),
    ('异常处理', 'PnP 求解失败或匹配点不足时跳过该帧并记录；优化不收敛时回退到优化前结果。'),
    ('后置条件', '生成带颜色的三维点云/网格模型及相机位姿。'),
])

caption('表 5-7 UC-07 模型保存与导出用例描述表')
usecase_table([
    ('用例名', '模型保存与导出'),
    ('简述', '将重建模型保存至模型库，并支持导出为 PLY/OBJ/glTF 等格式。'),
    ('主要参与者', '采集操作员、管理员。'),
    ('情景目标', '持久化并复用重建成果。'),
    ('前提条件', '重建完成，存在模型数据。'),
    ('触发事件', '用户点击“保存/导出”。'),
    ('场景描述', '选择模型 → 保存入模型库 → 选择导出格式 → 生成并下载文件。'),
    ('异常处理', '模型数据缺失或损坏时提示；导出失败时保留原始数据。'),
    ('后置条件', '模型入库并可被展示模块加载；导出文件可供外部软件（MeshLab/Blender）打开。'),
])

caption('表 5-8 UC-08 模型三维展示与交互用例描述表')
usecase_table([
    ('用例名', '模型三维展示与交互'),
    ('简述', '在 Web 前端以 Three.js 加载并展示三维模型，支持旋转、缩放、平移。'),
    ('主要参与者', '全部用户。'),
    ('情景目标', '直观呈现重建结果，供观察与演示。'),
    ('前提条件', '存在已保存的模型。'),
    ('触发事件', '用户打开模型展示界面。'),
    ('场景描述', '加载模型 → 渲染 → 用户拖拽旋转/滚轮缩放/平移交互。'),
    ('异常处理', '模型加载失败时提示并允许重试。'),
    ('后置条件', '模型成功渲染并可交互操作。'),
])

caption('表 5-9 UC-09 用户与系统管理用例描述表')
usecase_table([
    ('用例名', '用户与系统管理'),
    ('简述', '管理员对用户账号、系统配置与硬件设备进行管理。'),
    ('主要参与者', '管理员。'),
    ('情景目标', '维护系统账号与运行配置。'),
    ('前提条件', '管理员已登录。'),
    ('触发事件', '管理员进入系统管理界面。'),
    ('场景描述', '增删用户 → 配置系统参数 → 查看下位机在线状态与硬件信息。'),
    ('异常处理', '操作越权或参数非法时拒绝并提示。'),
    ('后置条件', '系统账号与配置更新生效。'),
])

h2('5.2 用例业务流程描述')
para('本节以活动流程方式描述核心用例的业务处理过程。')
h3('5.2.1 用户登录流程')
para('用户进入登录页 → 输入账号与密码 → 点击登录 → 系统校验身份 → 校验成功进入对应主界面，失败则弹出提示并返回登录页。')
h3('5.2.2 图像采集流程')
para('操作员发起采集 → 上位机经 TCP 下发采集参数 → 下位机多路摄像头同步采集 → 图像经 TCP 上传 → 上位机校验并入库 → 上报进度直至完成；若连接中断则触发重连续传。')
h3('5.2.3 三维重建流程')
para('操作员选择图像集并设置参数 → 提交重建任务 → 系统异步执行：图像处理（特征提取与匹配）→ 相机参数估计 → 稀疏点云 → BA 优化 → 密集重建 → 模型优化 → 生成模型并更新任务状态 → 前端提示完成并可进入模型展示。')
h3('5.2.4 模型导出流程')
para('用户选择模型 → 点击导出 → 选择目标格式 → 系统转换并生成文件 → 返回下载；若模型数据缺失则提示并终止。')

# ============================================================
# 6. 其他非功能需求
# ============================================================
h1('6. 其他非功能需求')

h2('6.1 性能需求')
para('系统的关键性能指标如下表所示。')
table([
    ['指标', '目标值'],
    ['用户界面响应时间', '常规操作 1 秒内响应'],
    ['单张图像特征提取', '秒级（本机 8 张约 8 秒，GPU 加速后更低）'],
    ['稀疏重建完整流程', '小数据集（≤20 张）分钟级内完成'],
    ['重投影误差', '稀疏重建平均重投影误差 ≤ 0.5 像素'],
    ['密集重建', '点云规模在内存约束内，支持降采样/分块'],
    ['系统稳定性', '长时间运行不崩溃，异常可恢复'],
], header=True, widths=[6.0, 9.6])
para('重建任务为计算密集型长任务，须异步执行并通过进度反馈避免界面阻塞。')

h2('6.2 安全措施需求')
bullet('用户密码错误超过限定次数后进行限流或锁定。')
bullet('系统对持久化文件读写应确保数据不丢失，覆盖写入前先备份，异常断电时本地数据可恢复。')
bullet('删除图像或模型时应正确删除目标数据及其关联数据，删除前二次确认，删除后校验无误。')
bullet('系统应定期备份自身数据，保证出现问题时可从快照恢复。')

h2('6.3 安全性需求')
bullet('每个用户仅能进行其权限对应的操作，系统对用户进行鉴权与授权。')
bullet('系统不应直接将底层数据暴露给用户，确保用户隐私信息与模型数据不泄露。')
bullet('上下位机 TCP 通信应具备基本的完整性校验（如校验和/消息序号），防止传输损坏。')

h2('6.4 软件质量属性')
table([
    ['质量属性', '说明'],
    ['易用性', '界面简洁清晰，用户无需手册即可完成常规采集与查看操作'],
    ['可维护性', '高内聚低耦合，模块接口清晰，代码规范注释完善'],
    ['可扩展性', '模块化设计支持新增重建算法与采集设备'],
    ['可靠性', '平均无故障时间达到可接受水平，异常可捕获、可恢复、可重试'],
    ['可测试性', '遵循 TDD，核心算法与接口均可单元测试'],
    ['可复现性', '固定随机种子与参数，重建结果可复现'],
], header=True, widths=[3.6, 12.0])

h2('6.5 可拓展功能')
para('以下功能作为可拓展功能，视开发进度与成员能力选择性实现：')
bullet('引入深度学习特征（SuperPoint/SuperGlue）提升弱纹理场景匹配鲁棒性。')
bullet('引入 COLMAP/OpenMVS 作为密集重建后端，进一步提升模型精度与纹理质量。')
bullet('引入 NeRF / 3D Gaussian Splatting 等神经渲染方法进行新视角合成。')
bullet('支持多用户并发重建任务与分布式计算。')
bullet('对模型与图像数据进行加密存储。')

doc.save('多视觉融合的三维重建平台搭建-需求分析报告.docx')
print('OK: 文档已生成')
